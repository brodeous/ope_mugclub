import 'package:flutter/material.dart';

class Styles {

    static TextStyle primaryHeader = TextStyle(
        fontSize: 32,
    );

    static TextStyle secondaryHeader = TextStyle(
        fontSize: 24,
    );

    static TextStyle displayVisits = TextStyle(
        fontSize: 64,
    );
}
